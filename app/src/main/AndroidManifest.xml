<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.tourguideapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RangitotoActivity" />
        <activity android:name=".SkyTowerActivity" />
        <activity android:name=".PihaActivity" />
        <activity android:name=".TawharanuiActivity" />
        <activity android:name=".OmahaActivity" />
        <activity android:name=".KawauActivity" />
        <activity android:name=".WaihekeActivity" />
        <activity android:name=".BridgeActivity" />
        <activity android:name=".ViaductActivity"></activity>
    </application>

</manifest>